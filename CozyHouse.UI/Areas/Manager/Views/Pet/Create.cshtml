@model Pet
@{
    ViewBag.Title = "Create";
}

<form method="post" enctype="multipart/form-data" asp-area="Manager" asp-controller="Pet" asp-action="CreatePet">
    <h2>Pet information</h2>
    <div><input type="text" placeholder="Pet Name" asp-for="Name" /></div>
    <div><input type="text" placeholder="Pet Age" asp-for="Age" /></div>

    <div>
        <label>Select Pet Type</label>
        <select asp-for="Type">
            @foreach (PetType type in Enum.GetValues(typeof(PetType)))
            {
                <option value="@type">@type.ToString()</option>
            }
        </select>
    </div>
    <div>
        <label>Select image of your pet</label><br />
        <input type="file" name="file" />
    </div>
    <button type="submit">Create!!!</button>
</form>